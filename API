import json
import requests
from Tkinter import *
import operator

parameters = {}

response = requests.get("https://api.myjson.com/bins/13vgkf", params=parameters)
data = json.loads(response.text)


def show_results():

    result = {}

    for TYPE in result:
        info = TYPE[1]
        MATCH1 = info['MATCH1']
        MATCH2 = info['MATCH2']
        print(MATCH1)
        print(MATCH2)
        print("")

show_results()
